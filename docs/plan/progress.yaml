# Build Loop — Progress State
# Schema: config + state read every iteration. Validate with yamllint.

config:
  verify: "npx tsc --noEmit && npm run test && npm run test:e2e"
  max_tasks_per_phase: 200
  quality_check_suppressions: []
  build_loop_version:
    init_version: 13
    builds: [13]

state:
  # --- Lifecycle (owner: iterator) ---
  phase: foundation
  phase_complete: false
  status: running

  # --- Task orchestration (owner: iterator) ---
  task_number: 13
  next_task: "[US-04] Wire pointermove to the hover state machine + Playwright coverage. (1) src/scene.ts: add raycastPointer(clientX, clientY) to SceneBundle that converts client coords to NDC using canvas bounding-rect and raycasts against grid.tileMeshes; returns {tileX, tileY} | null. (2) src/input.ts: extend AttachInputHandlersOptions so the caller provides raycastPointer. Add createPointerMoveHandler that (event) => calls handlePointerMove(state, raycastPointer(event.clientX, event.clientY)) via setState; attach to canvas.addEventListener('pointermove', ...) (NOT window); extend detach() to also removeEventListener pointermove. (3) src/main.ts: pass bundle.raycastPointer into attachInputHandlers. (4) src/input.test.ts: cover pointermove — cursor coords that resolve to a tile dispatch handlePointerMove and update state; cursor coords that return null clear hoveredTile to null; attach/detach registers and removes BOTH keydown AND pointermove listeners (listener count goes 0->2->0). (5) tests/e2e/foundation.spec.ts: new tests asserting — initial idle debug.ghost.visible===false, debug.ghost.material.opacity in [0.35,0.45], debug.ghost.material.transparent===true; press '1', move mouse to center of canvas (viewport), debug.ghost.visible===true + emissive '00e5ff' + hoveredTile non-null; press '2' without moving — ghost emissive flips to 'ff5a1f' (L50); move to tile A then tile B — the tileColors[idxA] returns to '#0a0a0a' in same frame tileColors[idxB] becomes '#0d4d57' (L48, L81); Escape returns ghost.visible===false and all tileColors back to '#0a0a0a' (L52); move cursor off the grid (way above canvas) — hoveredTile===null and ghost.visible===false (L51); rapid mouse sweep (20 move calls inside 200ms) does not throw and ends with ghostCount===1 (L54). Closes L47-L54, visible slices of L72-L74, L81-L82; and the mousemove portion of L92."
  last_result: pass
  consecutive_fails: 0

  # --- Review (owner: iterator) ---
  review_stage: null
  review_complete: false
  pr_number: null

  # --- Retrospective (owner: retro) ---
  retro_complete: false

  # --- Tracking (owner: iterator) ---
  completed_stories: []

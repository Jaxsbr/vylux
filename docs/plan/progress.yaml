# Build Loop — Progress State
# Schema: config + state read every iteration. Validate with yamllint.

config:
  verify: "npx tsc --noEmit && npm run test && npm run test:e2e"
  max_tasks_per_phase: 200
  quality_check_suppressions: []
  build_loop_version:
    init_version: 13
    builds: [13]

state:
  # --- Lifecycle (owner: iterator) ---
  phase: foundation
  phase_complete: false
  status: running

  # --- Task orchestration (owner: iterator) ---
  task_number: 17
  next_task: "[US-01,US-02,US-03,US-04,US-05] Manual doc fill — replace the three TODO sections in docs/manual/foundation.md (Controls, Hover preview, Click to place) with full prose covering: (a) keyboard controls 1/2/Esc + cursor toggle (US-03); (b) hover preview — dim faction highlight, translucent ghost unit, faction hex values, occupied tile suppresses ghost, ghost re-emerges on re-enter (US-04); (c) click to place — left-click commits, enforces occupancy (one unit per tile), click-outside-grid exits to idle, right/middle-click ignored, placed mesh is solid+emissive vs. ghost translucent (US-05). Cross-reference module layout (placement.ts state ownership, input.ts dispatcher, scene.ts reconcile) and the debug hook shape (window.__vylux.debug.placedMeshes, ghost, tileColors) so a reader can self-verify. No code changes; doc-only. Closes L86 for all five stories simultaneously, which unblocks the Story Completion Gate for US-01..US-05."
  last_result: pass
  consecutive_fails: 0

  # --- Review (owner: iterator) ---
  review_stage: null
  review_complete: false
  pr_number: null

  # --- Retrospective (owner: retro) ---
  retro_complete: false

  # --- Tracking (owner: iterator) ---
  completed_stories: []

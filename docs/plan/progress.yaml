# Build Loop — Progress State
# Schema: config + state read every iteration. Validate with yamllint.

config:
  verify: "npx tsc --noEmit && npm run test && npm run test:e2e"
  max_tasks_per_phase: 200
  quality_check_suppressions: []
  build_loop_version:
    init_version: 13
    builds: [13]

state:
  # --- Lifecycle (owner: iterator) ---
  phase: foundation
  phase_complete: false
  status: running

  # --- Task orchestration (owner: iterator) ---
  task_number: 9
  next_task: "[US-03] Wire keyboard input to the placement state machine. Create src/input.ts with createKeydownHandler(options) returning a pure (event) => void (uses handleKey; sets canvas.style.cursor to 'none' in placement, 'default' in idle; only calls setState on a real transition) and attachInputHandlers(options) binding the handler to target.addEventListener('keydown', ...) and returning { detach() }. Update src/main.ts to own a mutable PlacementState ref seeded from INITIAL_STATE and wire attachInputHandlers with target=window and setState that updates both the ref and hook.state. Update src/debug.ts VyluxHook.state type to PlacementState and seed hook.state from INITIAL_STATE. Add src/input.test.ts: createKeydownHandler cases ('1' -> placement+blue, cursor 'none'; '2' -> red; 'Escape' -> idle, cursor 'default'; unhandled key leaves both alone); attachInputHandlers detach() removes the listener (mock target Pick<Window, 'addEventListener'|'removeEventListener'>, assert listener count drops to 0 and post-detach dispatch is a no-op). Extend tests/e2e/foundation.spec.ts (or new placement.spec.ts): initial state idle/null + cursor 'default'; '1' -> placement+blue + cursor 'none'; '2' switches faction to red; 'Escape' returns to idle/null + cursor 'default'. Closes US-03 L42, L43, L94, and the keydown portion of L92."
  last_result: pass
  consecutive_fails: 0

  # --- Review (owner: iterator) ---
  review_stage: null
  review_complete: false
  pr_number: null

  # --- Retrospective (owner: retro) ---
  retro_complete: false

  # --- Tracking (owner: iterator) ---
  completed_stories: []

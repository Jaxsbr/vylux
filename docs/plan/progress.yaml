# Build Loop — Progress State
# Schema: config + state read every iteration. Validate with yamllint.

config:
  verify: "npx tsc --noEmit && npm run test && npm run test:e2e"
  max_tasks_per_phase: 200
  quality_check_suppressions: []
  build_loop_version:
    init_version: 13
    builds: [13]

state:
  # --- Lifecycle (owner: iterator) ---
  phase: foundation
  phase_complete: false
  status: running

  # --- Task orchestration (owner: iterator) ---
  task_number: 12
  next_task: "[US-04] Scene reconciliation for hover preview + ghost mesh + debug surface. (1) src/scene.ts: build one persistent ghost Mesh with BoxGeometry(0.8,0.8,0.8), MeshStandardMaterial{ transparent:true, opacity:0.4, emissive: cyan, emissiveIntensity:1 }, positioned y=0.5, initially invisible, added to scene (NOT to grid.group). (2) Add reconcile(state: PlacementState) to SceneBundle: track lastHoverCoord in closure; on each call, reset the previously-hovered tile material color back to GRID_CONSTANTS.tileColor, then — if mode==='placement' && hoveredTile && selectedUnitType — set hoveredTile material.color to hoverColorFor(faction) AND if NOT isTileOccupied then ghost.visible=true, ghost.position = tileToWorld(x,y) with y=0.5, ghost.material.emissive.set(ghostEmissiveFor(faction)); else ghost.visible=false. (3) Extend VyluxDebug with ghost: { visible, material: { emissive, opacity, transparent } } and ghostCount: number (count meshes named 'ghost' in scene, must be 1). (4) Change buildDebugSnapshot tileColors to iterate bundle.grid.tileMeshes LIVE (reading material.color.getHexString()) instead of the stale static array — so hover mutations show up. Drop grid.tileColors from GridBundle (no longer needed) or keep as build-time defaults but don't use in debug. (5) Wire bundle.reconcile(state) into main.ts per-frame animate() before render, passing the current state ref. (6) Vitest: extend src/scene.test.ts or add src/reconcile.test.ts — but since reconcile is impure (mutates meshes), use pure helpers: add computeGhostView(state) and computeHoverView(state) to src/placement.ts returning plain view objects, cover them with Vitest. Playwright comes in the next task (input wiring). Closes US-04 L47-L53 (visual pieces — but Playwright portion lands with input wiring), L72-L74 (ghost material hex + opacity), L81-L82 (hover tile material hex)."
  last_result: pass
  consecutive_fails: 0

  # --- Review (owner: iterator) ---
  review_stage: null
  review_complete: false
  pr_number: null

  # --- Retrospective (owner: retro) ---
  retro_complete: false

  # --- Tracking (owner: iterator) ---
  completed_stories: []

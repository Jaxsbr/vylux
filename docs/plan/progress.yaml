# Build Loop — Progress State
# Schema: config + state read every iteration. Validate with yamllint.

config:
  verify: "npx tsc --noEmit && npm run test && npm run test:e2e"
  max_tasks_per_phase: 200
  quality_check_suppressions: []
  build_loop_version:
    init_version: 13
    builds: [13]

state:
  # --- Lifecycle (owner: iterator) ---
  phase: foundation
  phase_complete: false
  status: running

  # --- Task orchestration (owner: iterator) ---
  task_number: 16
  next_task: "[US-05] Click wiring + Playwright coverage. (1) src/placement.ts: add pure handleClick(state, hit: TileRef|null, button: number): PlacementState — button !== 0 -> same ref; mode !== 'placement' -> same ref; hit === null -> exit to idle (mode='idle', selectedUnitType=null, placedUnits unchanged, hoveredTile preserved); otherwise delegate to tryPlace(state, hit.tileX, hit.tileY) and return result.state (ok=true -> fresh idle+placed; ok=false -> same ref for occupied/out-of-bounds). (2) src/input.ts: add createPointerDownHandler({ getState, setState, raycastPointer, canvas }) that reads event.button + event.clientX/Y, raycasts, calls handleClick, short-circuits when next === current, else setState(next) and toggles canvas.style.cursor to 'default' whenever next.mode === 'idle'. Extend attachInputHandlers to bind 'pointerdown' on canvas and include it in detach(). (3) src/main.ts: no shape change — already passes raycastPointer + canvas. (4) Vitest: +7ish cases on handleClick (left+placement+hit empty, left+placement+hit occupied, left+placement+null hit exits idle, left+idle no-op, right-click no-op, middle-click no-op, out-of-bounds hit returns same ref). +3 cases on createPointerDownHandler (left-click dispatches + updates cursor on idle, right-click no-op, same-ref no setState). Extend attachInputHandlers test to assert pointerdown listener registered on canvas + unbound by detach(). (5) Playwright (+5 tests): (a) press '1', move to tile center, left-click -> placedUnits.length===1 with { tileX, tileY, type:'blue' }, mode 'idle', ghost hidden, cursor 'default', debug.placedMeshes[0].material.emissive==='00e5ff', opacity===1, transparent===false; (b) press '2', left-click -> placedUnits adds { type:'red' } with emissive 'ff5a1f'; (c) with an occupied tile, press '1', hover + click same tile -> placedUnits.length unchanged AND mode stays 'placement'; (d) press '1', left-click at viewport corner outside the grid -> mode exits to 'idle' with placedUnits unchanged; (e) press '1', right-click AND middle-click -> placedUnits unchanged, mode stays 'placement'. Closes US-05 L61-L66, click portion of L92, and the observable slice of L77-L80 (Playwright now asserts placedMeshes material per-faction)."
  last_result: pass
  consecutive_fails: 0

  # --- Review (owner: iterator) ---
  review_stage: null
  review_complete: false
  pr_number: null

  # --- Retrospective (owner: retro) ---
  retro_complete: false

  # --- Tracking (owner: iterator) ---
  completed_stories: []

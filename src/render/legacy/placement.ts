// Type-only shim: legacy mesh files import FactionId from this path.
// In the prototype this came from a state machine; here we just need
// the type to keep the legacy build* signatures compatible.
export type FactionId = 'blue' | 'red';
